package io.github.yoonseo.pastelplugin.commands

import org.bukkit.command.Command
import org.bukkit.command.CommandExecutor
import org.bukkit.command.CommandSender
import org.bukkit.command.TabCompleter
/*
class PastelPluginCommand : CommandExecutor, TabCompleter{
    override fun onCommand(sender: CommandSender, command: Command, label: String, args: Array<out String>?): Boolean {









        return true
    }

    override fun onTabComplete(
        sender: CommandSender,
        command: Command,
        label: String,
        args: Array<out String>?
    ): MutableList<String> {

        //sender.sendMessage("label : $label, command: $command, args: $args")
        args ?: return mutableListOf()

        if(args.size >= 1) when(args[0]){
            "setChair" -> CommandHandler.
        }


    }
}*/